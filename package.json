{
  "name": "rozzhig",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass": "sass --watch .",
    "wp-sass": "sass --style=compressed --watch components:components --watch blocks:blocks --watch assets/styles/main.scss:assets/styles/main.css"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "sass": "^1.80.6"
  }
}
